<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.choices">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Rhothomir.CharacterSelectRecyclerView.Character_Select"></activity>
        <activity android:name=".Rhothomir.Rhothomir" />
        <activity android:name=".Fantasy.Fantasy_Battle" />
        <activity android:name=".Fantasy.Fantasy_Event" />
        <activity android:name=".GettingReady.Alan_Event" />
        <activity android:name=".Story_Select">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>